# Copy to .env for local development:  cp .env.example .env

# Your Anthropic API key. Get one at https://console.anthropic.com
# If left blank, Reelmint runs in a built-in DEMO mode (no real AI calls).
ANTHROPIC_API_KEY=

# Model to use for all AI generation.
AI_MODEL=claude-opus-4-8

# Port (Render sets this automatically in production).
PORT=3000

# Set to 1 to disable the free-tier watermark everywhere.
REELMINT_NO_WATERMARK=

# Optional: plug in any image-generation endpoint.
# It should accept JSON {prompt, width, height} and return {url} or {b64}.
IMAGE_API_URL=
IMAGE_API_KEY=
